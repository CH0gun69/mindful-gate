// Placeholder — kept so this folder exists in git (SwiftPM/git don't track empty
// directories) and so it stays a real .swift source file, not a README.md, which
// SwiftPM would flag as an "unhandled file" inside a target's source tree.
//
// This folder mirrors prototype/core/: it will hold MockData.swift (porting
// prototype/core/mock_data.py) and Strings.swift (porting prototype/core/strings.py).
// Delete this file once real content lands here.
